
export class ImageRoomModel {
    constructor(id: number, name: string, url: string) {
        this.id = id
        this.name = name
        this.url = url
    }
    public id: number
    public name: string
    public url: string
}