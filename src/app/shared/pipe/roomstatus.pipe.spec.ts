import { RoomstatusPipe } from './roomstatus.pipe';

describe('RoomstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
